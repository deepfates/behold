{
  "name": "behold",
  "version": "0.1.0",
  "description": "Minecraft bot using Mineflayer with OpenRouter chat replies",
  "private": true,
  "main": "src/index.js",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/src/index.js",
    "swarm": "npm run build && node dist/scripts/swarm.js",
    "dev": "ts-node --transpile-only src/index.ts",
    "lint": "eslint .",
    "format": "prettier -w .",
    "check": "eslint . && prettier -c .",
    "prepare": "husky install",
    "cli": "ts-node --transpile-only src/tools/readline.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "canvas": "^2.11.2",
    "dotenv": "^16.4.5",
    "minecraft-data": "^3.66.0",
    "mineflayer": "^4.20.0",
    "mineflayer-pathfinder": "^2.4.5",
    "prismarine-viewer": "^1.33.0",
    "vec3": "^0.1.10"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.4.0",
    "@commitlint/config-conventional": "^19.2.2",
    "eslint": "^9.11.1",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "ts-node": "^10.9.2",
    "@types/node": "^22.7.7"
  },
  "lint-staged": {
    "**/*.{js,ts,jsx,tsx,json,md}": [
      "prettier -w",
      "eslint --fix --max-warnings=0",
      "git add"
    ]
  }
}