# Compact local resident-runtime frontier

## Verdict

Of the installed compact candidates inspected under loopback-only, provider-free
conditions, only `qwen/qwen3-vl-4b@4bit` passed the narrow pre-live screen for
an embodied Oxford treatment. A stable-prefix readiness call outside world
release reduced two fresh decisions to 2.27 and 3.32 seconds, produced grounded
original chat, reported zero reasoning tokens, and passed the unchanged strict
schema. The production broker and resident-mind path then reproduced the
addressed-chat decision in 3.42 seconds after a 2.27-second authority-free setup
call.

This admitted Qwen to a live treatment; it was not evidence of sustained
conduct. The subsequent two-episode live/resume treatment recovered correctly
after exposing one prefix-readiness defect, but every one of 47 completed Qwen
actions was the same camera sweep and shared live latency degraded materially.
That negative result is recorded in
[Qwen Oxford live and resume treatment](2026-07-26-qwen-oxford-live-resume.md).
Bonsai 1.7B was fast but copied perception and another resident's
speech into its own chat. Bonsai 8B was incompatible with the installed MLX
engine. SmolLM3's installed template defaults to explicit reasoning, which the
unchanged resident charter does not disable. None of those three was admitted
to Minecraft.

## Candidate boundary

All inference used exact retained Oxford human-semantic requests with no world
or Minecraft authority, provider, download, retry, correction, normalization,
substitution, or action steering. The two source wires were deliberately fresh:

- `0bc877130e966f2d3250397db9b845135fe1d932f4aa6083406e7210f619c9be`
  showed Qwen nearby with no earlier resident action continuity.
- `7079bb94c4e636f5566045fbc383119e19f820c7a082ade43edc580dfae05a24`
  added one newly addressed message from Qwen.

This avoids interpreting another response from the earlier six-turn repeated
camera sequence as evidence of adaptive conduct.

## Rejected compact candidates

### Bonsai 8B MLX

The installed `prism-ml/Bonsai-8B-mlx-1bit` artifact remains excluded at setup.
Its exact tree is
`3d88bf0792324c222f91a76bbd848017b53f15bd422c96d18418822dc73236db`.
The installed MLX engine rejected its 1-bit tensors and reported support only
for 2, 3, 4, 5, 6, or 8 bits. No conversion, replacement artifact, or inference
under that identity was attempted.

### SmolLM3 3B MLX

The installed `lmstudio-community/SmolLM3-3B-MLX-8bit` artifact is 3,284,853,367
bytes. Its template SHA-256 is
`925f0abda99abd87857b2e49dc2fc0a453a958925c92c34c4fc57eb76c453d9a`.
The template sets `enable_thinking = true` when unspecified, injects
`Reasoning Mode: /think`, and requests a detailed `<think>` section. It switches
only when `/no_think` is present. Behold's unchanged resident charter contains
no such model-specific control token, so the artifact was excluded statically
rather than altering the treatment or eliciting private reasoning.

### Bonsai 1.7B GGUF

The exercised candidate was:

- index identity `prism-ml/Bonsai-1.7B-gguf/Bonsai-1.7B.gguf`;
- 248,302,272 bytes;
- file SHA-256
  `0ae245fc08236af7cb64caff164937e53a8d54af611b0f398cc992c0a5ba70c4`;
- regular-file tree SHA-256
  `4fc8ab8442545bd1550c0142f33acf68e113e26dc29e41987fc23215baa128bd`;
- embedded template SHA-256
  `30a75d10e60b57e2f260420163dd59720dacf9f63b9a8de070d65dd80a7b30f7`;
- LM Studio llama.cpp engine
  `llama.cpp-mac-arm64-apple-metal-advsimd@2.14.0`.

It returned in 2.606 seconds cold and 1.868 seconds warm, with zero reported
reasoning and validator-clean JSON. Behavior nevertheless failed the grounding
gate. For fresh co-presence it sent a chat beginning `New world experience:`
and copied observation protocol JSON. For the addressed chat it repeated
Qwen's incoming message verbatim, including `Hey Gemma`, while acting as Gemma.
Fast schema compliance is insufficient when the proposed public action is
copied from perception or another body's voice.

Retained ignored evidence:

- `data/lmstudio-runtime-frontier/bonsai-gguf-2026-07-26T14-26-14.853Z/result.json`
  — SHA-256
  `0bfeabcb5eaf3f1bdb46b1b2d6bd275e32da650766c86d5aa16471dff30b42a9`.
- Its LM Studio model log — SHA-256
  `ecd1ec8533ce2ed60c9d38d13574401f9740634b2003f23a5bd7d0e1410e3628`.

## Qwen body-horizon result

The admitted candidate remains the exact installed MLX artifact:

- model key `qwen/qwen3-vl-4b@4bit`;
- index identity
  `qwen/qwen3-vl-4b@lmstudio-community/Qwen3-VL-4B-Instruct-MLX-4bit`;
- 3,109,915,433 bytes;
- complete tree SHA-256
  `28f19c53f352d1ffd6238f93c1ba26f0df363e18a5e2aa05a70390842e65332c`;
- template SHA-256
  `3636d0f0bd6bef02654cdffdc447b79cb2cef8ab02cc75267345946291a489e4`;
- LM Studio `0.4.12+1`, CLI `0b2a176`, MLX engine
  `mlx-llm-mac-arm64-apple-metal-advsimd@1.10.1`.

One 2.181-second setup request contained only the exact stable charter/action
contract prefix and the literal readiness question. It had no observation,
tools, resident-decision response schema, action authority, or use as a later
decision. The following fresh choices were:

| Horizon                   | Wall time | Public action                                                     |
| ------------------------- | --------: | ----------------------------------------------------------------- |
| nearby Qwen, no history   |   2.270 s | `chat` with `Hi Qwen!`                                            |
| newly addressed Qwen chat |   3.319 s | original acknowledgment of Qwen and the visible stone/glass scene |

Both outputs reported zero reasoning tokens and matched the exact resident
schema. Retained result SHA-256:
`05042ee00e9de9711370b66de8ad55d6474497619b847cd315d9a6074f3e45f2`;
model-log SHA-256:
`674151397daf49dd09ee6db6b20e92d138ee94d5dfebb9531a6031127f6030cd`.

The final production-path exercise used the authenticated broker, quota ledger,
transport capture, exact resident mind, and the retained addressed-chat source.
Preparation completed before the measured decision horizon in 2.274 seconds;
the released decision completed in 3.420 seconds. It again produced an original
grounded chat response, reported zero readiness and decision reasoning tokens,
used exactly two physical `resident_decision` charges, captured two attempts,
and left LM Studio inventory empty. The preparation and action stable-prefix
hashes were byte-identical at
`cfc5879b939276c9f5d8e619a08010f437a387bf4fa0d8322e2271ffefeef746`.

Retained production result SHA-256:
`867bb3203de1477ec374a4cbc9d8f9e6fe1fd8c254cdf028f2b919a3dc9e2b5b`;
broker-journal SHA-256:
`5ae023f27e4b8e015c09fa442f364461d4279683947ebaf21542daa204dd4192`.

## Implemented product boundary

LM Studio minds now expose an optional authority-free preparation step. During
an ordinary managed run, the controller performs it while Minecraft ticks are
still frozen and before it arms the population release. The readiness response
is journaled but cannot become an action. An urgent resident request must
already have an exact matching action-contract and stable-prefix readiness
record or action inference is refused without a repair or in-horizon rewarm. A
later deliberative contract may establish its own authority-free prefix before
its action request; readiness records are keyed by exact contract identity
rather than being incorrectly frozen to the body's first setup affordances.

The broker recognizes `resident_prefix_readiness`, validates its exact
three-message/no-tool/16-token wire, retains its purpose in transport evidence,
and charges the physical attempt to the existing resident-decision quota. The
release gate accepts only one completed readiness attempt per LM Studio
resident, zero decision or fold purposes, zero active/failed/cancelled/rejected
work, and the matching one-attempt accounting delta. Fixed four-slot pilots
therefore require five LM Studio decision-attempt charges: one setup readiness
and four released opportunities.

The resident action and loom-fold parsers now reject nonempty
`reasoning_content`, and usage evidence retains nested reasoning-token counts.
LM Studio session startup also requires two stable HTTP inventory reads after
load; this addresses an observed transient connection reset when switching
runtime families without treating setup failure as inference evidence.

## What remains unknown

No compact candidate in this report has demonstrated sustained adaptive
Minecraft conduct, consequence-sensitive continuation, resident-authored world
change, or useful social behavior. Qwen has now exercised clean resume, but its
47 completed live actions across the initial episode and resume were all the
same look-around camera action. Prefix readiness establishes latency and exact
request identity, not a cache-hit claim: LM Studio did not report cached prompt
tokens.

The policy binds context length, output cap, temperature, runtime, template, and
artifact identity. It does not yet bind every possible LM Studio/model default
such as seed, top-p, top-k, min-p, or repetition controls. Those defaults must
not be described as measured or fixed. The Oxford resume tested two residents
sharing one Qwen instance with cognition concurrency two: 25 of 27 decisions
exceeded five seconds, so the narrow fresh-wire timing did not survive that
workload. Native human co-presence remains an explicit owner-held treatment and
is outside this frontier.
