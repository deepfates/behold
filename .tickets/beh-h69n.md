---
id: beh-h69n
status: closed
deps: [beh-hi81]
links: []
created: 2026-08-01T04:15:17Z
type: task
priority: 1
assignee: deepfates
parent: beh-kpqx
tags: [resident-interface, continuity, prompt]
---

# Converge on the smallest ordinary resident choice contract

The current default path supplies strategy and progress machinery, while the ordinary legible path requires an intention and expected observable consequence inside every action tool. Converge on a versioned ordinary choice contract that returns one admitted action with its arguments or yields, preserves actual action and consequence for continuity, and contains no controller-supplied goal, recovery, progress judgment, or compulsory private/public reasoning. Preserve old treatment and history parsing under their existing names.

## Acceptance Criteria

One versioned ordinary treatment presents persistent identity, bounded own experience, independent others, bodily controls as attempts, and Minecraft consequences as authority; it asks for exactly one admitted action or an explicit yield. Its ordinary wire contains no task, project, progress guard, recovery choice, required action, intention, expected consequence, or private reasoning. Supported local and provider-compatible transports parse the same semantic choice while keeping resident sessions isolated. Lync retains the selected action, actual consequence, and next experience; old profile histories remain readable and explicitly non-default. Focused tests prove those absences and causal bindings. Before any model calls, record exact request-size and schema differences from `legible-resident-v1`; use a bounded recognition or operator-legibility probe only if it could reverse removal of compulsory narration, without grading conduct.

## Implementation checkpoint

- Added `resident-v2` as the ordinary runtime default over the human-semantic body/action surface and vanilla risk. Its five-line charter supplies persistent own-life identity, bounded current experience, independent others, controls as attempts, and Minecraft consequence authority. Runtime admission refuses controller tasks; progress guards, project tools, recovery choices, and required actions remain absent.
- Added separately versioned action-only stable sessions for LM Studio and OpenRouter v2 strict JSON. Both require one admitted action, including explicit `wait_for_event` yield, and carry only that action's arguments. They reject profile/schema drift, assistant prose, multiple choices, unadmitted actions, and invalid inputs. The narrated `legible-resident-v1` protocols and histories remain unchanged.
- Preserved the low-level policy constructor's legacy default for explicit old callers and fixtures; ordinary CLI/live composition passes the runtime-resolved `resident-v2` profile. This avoids silently reinterpreting retained tests or histories.
- Re-rendered retained OxfordIris request blob `16d759f3b3e11cd7b268d893e339eb78b6fcb7fdfa362295cbda2d7d3cf6842c` with the same 18 actions and dynamic context, without a provider call. The narrated body was 33,387 bytes (17,624 messages; 15,498 tools). `resident-v2` was 17,855 bytes (12,496 messages; 5,094 tools), saving 15,532 body bytes, including 10,404 tool-definition bytes.
- Focused tests cover the charter's prohibited strategy/narration, runtime default and task rejection, exact provider strict-session wire/decision, exact LM Studio schema/prefix/wire, and legacy treatment mismatch. Full verification: `npm run lint` passes; `npm test` passes 633 with 1 skipped and 0 failed.
- Still unexercised here: a real model response and the persistent-world live/lens/stop/resume path. Those are owned by `beh-rhxm`, not this no-world contract ticket.

## Adversarial audit resolution

Read-only review found three contract-boundary defects after the first green
implementation. They now fail closed:

- current Ollama transports reject `resident-v2` and remain explicitly legacy;
- mixed-profile Lync is preserved unchanged while `resident-v2` receives a
  deterministic, profile-bound factual projection of chosen controls, typed
  settlements, communication, and coarse later perception; and
- each authenticated OpenRouter client pins its resident policy, so action-only
  and narrated schemas cannot be crossed under the same bearer.

Mixed-history restart and legacy-cache tests retain factual continuity, reject
the old projection cache without provider work, and recursively exclude the
legacy steering canaries. The model instance identity remains a physical-weight
identity rather than a resident-memory identity.

## Read-only audit checkpoint

- No current test or exercised run demonstrates that either compulsory field improves later resident recognition or operator comprehension. Existing tests establish exact schema, persistence, replay, projection, and tamper rejection only.
- On the retained 18-control OxfordIris request reconstructed through current native v2, the two fields add exactly 8,622 schema bytes: 54.7% of tool-definition bytes and 25.5% of the 33,808-byte request. The same schema increment applies to the strict/local envelope. The retained Luna output carried 198 bytes of narration beyond its action-only arguments.
- Neither field participates in action authorization, selection, execution, consequence validation, or conduct grading. Lync and Textile already render action, outcome, and before/after perception when utterance is absent. Working continuity independently retains the chosen action and actual consequence; narration can displace older causal turns under its byte cap.
- Existing live evidence is mixed-to-negative rather than dispositive: two valid legible commitments did not develop into adaptive conduct, malformed expected-consequence prose rejected otherwise-social attempts, and a later two-field Gemma life still repeated camera actions.
- Still required before model calls: freeze the exact action-only and two-field wire contracts, source request/history hashes, route/model/settings, call order, repetitions, cap, factual recognition probe, and stop/decision thresholds. Proposals remain authority-free and are discarded.
